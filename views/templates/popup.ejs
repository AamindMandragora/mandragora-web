<div id="shade"></div>
<div id="<%= title.toLowerCase() %>">
    <h1><%- title %></h1>
    <button id="exit">X</button>
    <% for (var i = 0; i < elements.length; i++) { %>
        <<%- elements[i][0] %> id="<%= elements[i][1] %>"><%= elements[i][2] %></<<%= elements[i][0] %>>
    <% } %>
</div>